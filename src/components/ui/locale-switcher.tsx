/**
 * Locale Switcher - Simplified for English Only
 * Returns null since we're using English only now
 */

'use client';

export function LocaleSwitcher() {
  // Disabled - English only mode
  return null;
}
